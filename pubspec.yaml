name: dfu_unlocker
description: "DFUUnlocker beta — cross-platform desktop unlocking toolkit."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/featured_phone_specs.json
    - assets/phones/
